#include<math.h>
void prime(int , int);

int main(void) {
	int t,h;
	scanf("%d\n",&t);
	for(h=0;h<t;h++)
	{
		int start,end,n;
		scanf("%d %d",&start,&end);
		while(start<=end)
		{
			n=sqrt(start);//A number is prime if it is not divisible by numbers from 2 to sqrt(number):-)
			prime(start,n);
			start++;
		}
	}

	return 0;
}
void prime(int start, int n)
{
	int i,flag=1;
	for(i=2;i<=n;i++)//A number is prime if it is not divisible by numbers from 2 to sqrt(number):-)
	{
		if(start%i==0)
		{
			flag=0;
			break;
		}
	}
	if(flag==1&&start!=1)
	printf("%d\n",start);
}
